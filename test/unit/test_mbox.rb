require "test_helper"

describe Redwood::MBox do
  it "can index an mbox file" do
    assert_equal(false, true)
  end
  it "can index an mbox file with special characters" do
    assert_equal(false, true)
  end
  it "can display an email from an mbox file" do
    assert_equal(false, true)
  end
  it "can display an email from an mbox file with special characters" do
    assert_equal(false, true)
  end
end

