require "test_helper"

describe Redwood::Maildir do
  it "can index an maildir directory" do
    assert_equal(false, true)
  end
  it "can index an maildir directory with special characters" do
    assert_equal(false, true)
  end
  it "can display an email from an maildir directory" do
    assert_equal(false, true)
  end
  it "can display an email from an maildir directory with special characters" do
    assert_equal(false, true)
  end
end

